# Web Application

FastAPI routes and dependency wiring for the LinkedIn Poster web interface live here. The module uses the shared `LLMService` façade to fulfil requests and should remain free of direct model/runtime logic.
