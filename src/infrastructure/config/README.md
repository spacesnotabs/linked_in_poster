# Configuration Infrastructure

Modules here load environment-driven settings and model catalogues. `settings.py` exposes `AppSettings` plus helper functions to read `config/model_config.json` with sensible defaults.
