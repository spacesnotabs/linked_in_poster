# Support Utilities

The support package collects cross-cutting helpers (logging setup, serialization utilities, tracing hooks) that multiple layers may share. Keep dependencies minimal and avoid embedding business rules here.
